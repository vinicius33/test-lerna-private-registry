{
  "name": "@logistics/utils",
  "version": "0.0.0",
  "private": true,
  "publishConfig": {
    "registry": "https://nexus.usehurrier.com/repository/logistics-npm-private/:_authToken=${NEXUS_NPM_TOKEN}"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna:bootstrap": "lerna bootstrap",
    "lerna:release": "lerna publish --no-verify-access"
  },
  "devDependencies": {
    "lerna": "^3.10.7"
  }
}
